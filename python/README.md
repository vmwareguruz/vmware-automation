# Python Scripts

This folder contains Python-based automation for VMware environments.

Suggested structure:
- `api-examples/` – sample REST API scripts
- `vcenter/` – vCenter automation using APIs/SDKs
- `esxi/` – host-level operations
- `multithreaded/` – parallel task execution
- `json-driven/` – configuration/JSON-driven automation workflows
